# YouTube Summary Intermediate Entities Design

Date: 2026-06-16

Status: approved in design discussion. This document defines the next pipeline
slice after the YouTube Summary transcript-analysis and synthesis MVP.

## Source Documents

- `docs/superpowers/specs/2026-06-14-youtube-summary-prompt-pack-mvp-design.md`
- `docs/superpowers/plans/2026-06-15-youtube-summary-synthesis-stage.md`
- `docs/prompt-packs/youtube_summary_pack_spec.md`
- `docs/prompt-packs/validation_rules.md`
- `docs/prompt-packs/schemas/README.md`
- `src-tauri/src/prompt_packs/youtube_summary/synthesis_input.rs`
- `src-tauri/src/prompt_packs/result_builder.rs`
- `src-tauri/src/prompt_packs/validation.rs`
- `src-tauri/src/prompt_packs/runtime.rs`

## Problem

The current YouTube Summary pipeline already asks the transcript-analysis LLM
for `segment_candidates`, `key_point_candidates`, `quote_candidates`,
`claim_candidates`, and `evidence_fragment_candidates`. Downstream code mostly
uses only videos, claims, evidence, warnings, and synthesis text. The synthesis
prompt still tells the model to keep `claim_refs` and `evidence_refs` empty
because the backend does not expose an allowed reference map.

This leaves the pipeline with useful candidate material but no backend-owned
intermediate graph. Synthesis cannot safely cite claims or evidence, and later
features such as claim linking, validation findings, chunking, and richer UI
projections would have to infer structure from loose candidate arrays.

## Goals

- Build a backend-owned intermediate entity graph from successful
  transcript-analysis parsed outputs.
- Assign stable run-local refs for segments, key points, quotes, claims, and
  evidence.
- Preserve the rule that LLMs propose semantic candidates while the backend owns
  IDs and reference validity.
- Persist the graph as a stage or run artifact without introducing new LLM
  stages in this slice.
- Add a closed-world allowed-ref map to synthesis input.
- Allow synthesis output to use backend-provided `claim_refs` and
  `evidence_refs`.
- Add minimal ref-check validation for synthesis references.
- Keep the slice small enough to verify with existing Prompt Pack tests plus
  focused entity/ref-check tests.

## Non-Goals

- No chunking of long transcripts or playlists.
- No separate LLM execution stages for segment, key point, quote, action item,
  or open question extraction.
- No full claim-relation graph.
- No confidence scoring beyond preserving candidate data already returned by the
  transcript-analysis output.
- No complete `validation` pipeline stage row.
- No dedicated UI for browsing every intermediate entity.
- No large schema-table migration for all entity types unless implementation
  proves artifact-only persistence is insufficient.

## Current Pipeline Shape

Current execution shape:

```text
run snapshots
  -> transcript_analysis LLM output
  -> parsed_output artifact
  -> synthesis input built from loose transcript candidates
  -> synthesis LLM output
  -> canonical result builder
```

The synthesis input currently includes:

- per-video wrappers;
- `claim_candidates`;
- `evidence_fragment_candidates`;
- `warning_candidates`.

It does not expose backend-owned IDs for candidates, so synthesis cannot safely
cite them.

## Target MVP Shape

Target execution shape:

```text
run snapshots
  -> transcript_analysis LLM output
  -> parsed_output artifact
  -> backend intermediate entity graph
  -> intermediate_entities artifact
  -> synthesis input with canonical graph and allowed refs
  -> synthesis LLM output with checked refs
  -> canonical result builder
```

The first implementation should not add new queued LLM stages. It should use
the existing successful transcript-analysis outputs and build deterministic
backend-owned entities from them.

## Intermediate Graph

The graph is run-local. Its refs are stable within a run and are generated by
the backend in deterministic source/order traversal.

Recommended top-level artifact shape:

```json
{
  "stage_io_version": "1.0",
  "schema_version": "1.0",
  "graph_kind": "youtube_summary_intermediate_entities",
  "run_id": 42,
  "sources": [],
  "segments": [],
  "key_points": [],
  "quotes": [],
  "claims": [],
  "evidence": [],
  "warnings": [],
  "allowed_refs": {
    "source_refs": [],
    "segment_refs": [],
    "key_point_refs": [],
    "quote_refs": [],
    "claim_refs": [],
    "evidence_refs": []
  }
}
```

### Source Records

Sources mirror the run snapshot identity:

```json
{
  "source_ref_id": "source_ref_1",
  "source_snapshot_id": 1001,
  "title": "Video title"
}
```

### Segments

Segments come from `video_candidate.segment_candidates` when present.

```json
{
  "segment_ref": "segment_1",
  "source_ref_id": "source_ref_1",
  "order_index": 0,
  "title": null,
  "summary_text": "Segment summary",
  "material_refs": ["material_ref_1"]
}
```

If transcript output has no segment candidates, MVP should not synthesize fake
segments. The graph may have an empty `segments` array.

### Key Points

Key points come from `video_candidate.key_point_candidates`.

```json
{
  "key_point_ref": "key_point_1",
  "source_ref_id": "source_ref_1",
  "segment_ref": "segment_1",
  "text": "Important point",
  "material_refs": ["material_ref_1"]
}
```

`segment_ref` is optional. If a candidate does not clearly point to a segment,
the backend should store `null` rather than inventing a link.

### Quotes

Quotes come from `video_candidate.quote_candidates`.

```json
{
  "quote_ref": "quote_1",
  "source_ref_id": "source_ref_1",
  "segment_ref": "segment_1",
  "text": "Verbatim quote",
  "material_refs": ["material_ref_1"]
}
```

`segment_ref` is optional for the same reason as key points.

### Claims

Claims come from `claim_candidates`.

```json
{
  "claim_id": "claim_1",
  "source_ref_id": "source_ref_1",
  "text": "Claim text",
  "material_refs": ["material_ref_1"]
}
```

The backend keeps the existing `claim_id` naming convention used by the
canonical result builder, but moves ownership of ID assignment into the
intermediate graph.

### Evidence

Evidence comes from `evidence_fragment_candidates`.

```json
{
  "evidence_id": "evidence_1",
  "source_ref_id": "source_ref_1",
  "text": "Evidence text",
  "material_refs": ["material_ref_1"],
  "quote_ref": "quote_1"
}
```

`quote_ref` is optional. The backend may link evidence to a quote only when it
can do so deterministically from candidate data or exact text matching inside
the same source. Ambiguous matches remain unlinked.

## Persistence

MVP persistence should start with an artifact instead of a table migration.

Preferred artifact:

- `artifact_kind = "intermediate_entities"`
- `stage_run_id` may point to the synthesis stage when the graph is built as
  synthesis input preparation, or to the transcript stage when the graph is
  emitted per source. The first implementation should choose the smaller code
  path and document it in tests.
- `content_type = "application/json"`
- `compression_kind = "zstd"`

Artifact-only persistence is enough for the MVP because the immediate consumer
is backend synthesis input. Projection tables can follow when UI navigation or
filtering needs first-class rows.

## Synthesis Input Changes

`build_synthesis_stage_input` should include both the current loose candidate
view and the canonical graph during the transition.

Recommended new fields:

```json
{
  "canonical_graph": {
    "sources": [],
    "segments": [],
    "key_points": [],
    "quotes": [],
    "claims": [],
    "evidence": []
  },
  "allowed_refs": {
    "source_refs": [],
    "claim_refs": [],
    "evidence_refs": [],
    "quote_refs": []
  }
}
```

Synthesis prompt rules should change from "keep claim_refs/evidence_refs empty"
to:

- use only `claim_refs` from `allowed_refs.claim_refs`;
- use only `evidence_refs` from `allowed_refs.evidence_refs`;
- use only `source_refs` from `allowed_refs.source_refs`;
- do not create backend-owned IDs;
- leave refs empty when there is no supporting allowed ref.

`quote_refs` can be present in the input for model grounding, but synthesis
output does not need to expose quote refs in the first implementation unless the
existing synthesis schema already allows them.

## Minimal Validation And Ref Checks

This slice should add closed-world checks without creating a full validation
stage.

Intermediate graph checks:

- every `source_ref_id` exists in the run snapshot source registry;
- all backend-owned refs are non-empty and unique within their ref type;
- every `material_ref` is allowed by the source's frozen stage input;
- `key_point.segment_ref`, when present, references a segment from the same
  source;
- `quote.segment_ref`, when present, references a segment from the same source;
- `evidence.quote_ref`, when present, references a quote from the same source.

Synthesis output checks:

- every synthesis `source_refs` value is in `allowed_refs.source_refs`;
- every synthesis `claim_refs` value is in `allowed_refs.claim_refs`;
- every synthesis `evidence_refs` value is in `allowed_refs.evidence_refs`;
- synthesis must not include backend-owned object IDs such as `claim_id`,
  `evidence_id`, `theme_id`, `common_claim_id`, or `source_ref_id` as created
  objects;
- unknown refs should fail validation and quarantine the candidate through the
  existing quarantine path.

## Canonical Result Builder Changes

`build_youtube_summary_canonical_result` should prefer intermediate graph
entities when the artifact exists.

Initial behavior:

- build `claims` from graph claims;
- build `evidence` from graph evidence;
- keep the current fallback path from transcript parsed output for older runs or
  tests without an intermediate graph artifact;
- keep synthesis null/partial behavior unchanged.

This preserves old runs and avoids a hard cutover.

## Test Strategy

Add focused Rust tests for:

- deterministic ID assignment across multiple source snapshots;
- empty segment/key point/quote candidate arrays;
- invalid material refs rejected or quarantined through the existing validation
  path;
- duplicate backend refs impossible or rejected;
- key point cannot link to a segment from another source;
- evidence cannot link to a quote from another source;
- synthesis input contains `canonical_graph` and `allowed_refs`;
- synthesis output with unknown `claim_refs` fails validation;
- synthesis output with allowed refs succeeds;
- canonical result builder uses graph claims/evidence when graph artifact
  exists and falls back to old candidate parsing when it does not.

Existing broader checks remain:

```powershell
cargo fmt --manifest-path src-tauri\Cargo.toml --check
cargo test --manifest-path src-tauri\Cargo.toml --lib youtube_summary_
cargo test --manifest-path src-tauri\Cargo.toml --lib prompt_packs
cargo check --manifest-path src-tauri\Cargo.toml
git diff --check
```

## Rollout Plan

Implement this as small commits:

1. Add the intermediate graph builder module and pure tests.
2. Persist an `intermediate_entities` artifact during synthesis input
   preparation or immediately after transcript-stage success.
3. Add `canonical_graph` and `allowed_refs` to synthesis input.
4. Update the synthesis prompt to allow checked `claim_refs` and
   `evidence_refs`.
5. Add synthesis ref validation and quarantine tests.
6. Update canonical result builder to prefer graph entities with fallback.

## Open Decisions For Implementation

The implementation plan should choose one of these artifact ownership options:

- run-scoped graph artifact attached to the synthesis stage;
- per-source graph artifacts attached to transcript stages plus synthesis input
  merges them;
- both, with per-source artifacts as provenance and a merged synthesis artifact
  as the synthesis input contract.

Recommendation: start with a merged graph artifact attached to the synthesis
stage because it is the direct consumer and keeps the first slice smaller. Add
per-source provenance artifacts later if UI or debugging needs them.

