# YouTube Manual Verification

Date: 2026-05-09

Environment:

- Windows Tauri dev app.
- `yt-dlp --version`: `2026.03.17`.
- Runtime command returned `ytdlpAvailable = true` and message `yt-dlp is available`.
- Main live video fixture: source `30`, `https://www.youtube.com/watch?v=M7lc1UVf-VE`, title `YouTube Developers Live: Embedded Web Player Customization`.
- Main playlist fixture: source `32`, title `Manual Verification Playlist`.

Direct MCP/webview command calls return backend snake_case payloads; the Svelte API wrappers keep the user-facing TypeScript shape camelCase.

| Scenario | Input URL or source fixture | Steps | Expected result | Result | Notes |
| --- | --- | --- | --- | --- | --- |
| Public video with manual captions | `https://www.youtube.com/watch?v=M7lc1UVf-VE`, source `30` | Previewed the URL, added the source, synced transcript, inspected video detail. | Preview succeeds; add creates one YouTube video source; transcript sync creates a `youtube_transcript` item and segment rows; detail shows captions `synced`; `transcript_only` analysis can start. | Partial pass | Preview/add/sync/detail passed. Detail showed captions `synced`, `itemCount = 1`, `segmentCount = 466`. Analysis start was not run in this pass because it would require a live LLM report execution. |
| Public video with auto captions | Not selected in this pass | No stable auto-caption-only public fixture was exercised. | Transcript sync falls back to auto captions, metadata records `auto`, and detail shows captions `synced`. | Not run | Needs a known public video with no manual captions and stable auto captions. Automated corpus tests cover `is_auto_generated` segment metadata handling, but this live provider case remains open. |
| Video with no captions | Playlist fixture row `manual-unlinked-video` in source `32` | Inspected playlist detail for an unavailable unlinked row. | Transcript sync completes without panic; status becomes `no_captions` or equivalent; detail shows captions `unavailable`; analysis preflight excludes or clearly rejects empty transcript. | Partial pass | Fixture detail showed availability `no_captions`, `videoSourceId = null`, captions `unavailable`, and no transcript rows. Live no-captions sync and analysis preflight were not run. |
| Shorts URL | `https://www.youtube.com/shorts/M7lc1UVf-VE` | Previewed and added the Shorts URL after the watch URL source already existed; inspected detail. | URL parser maps the Shorts URL to the video source; add dedupes against the same video id; detail link uses a canonical YouTube URL. | Pass | Preview returned video id `M7lc1UVf-VE`; add deduped to existing source `30`. Latest detail canonical URL was `https://www.youtube.com/shorts/M7lc1UVf-VE`, which opens the same video source. |
| Live URL | No active `live_now` fixture selected | No active livestream URL was exercised. | Metadata records `live_now` or current availability; transcript sync does not spin; UI shows availability/provider warning. | Not run | Source `30` covered a live-ended availability state, not an active livestream. Needs a safe public live URL during the next provider pass. |
| Upcoming or live-ended source | Source `30` | Inspected preview/add/detail after metadata sync. | Upcoming videos are not treated as deleted; live-ended transcript-pending videos are marked `live_ended_transcript_pending`; retry controls follow retryable statuses from Part 3. | Partial pass | Detail showed `availabilityStatus = live_ended_transcript_pending` while captions were synced. Upcoming-video behavior and retry-control gating were not live-tested. |
| Public playlist | Source `32`, `Manual Verification Playlist` | Inspected playlist detail and summary counts. | Preview is bounded; metadata sync pages entries; detail shows ordered rows; linked count matches active linked rows; sync-all starts one playlist full-sync job. | Partial pass | Detail showed 2 ordered rows, `linkedVideoCount = 1`, `unavailableCount = 1`, and summary captions/comments rolled up from linked source `30`. Live public playlist preview/full paging and sync-all were not run in this pass. |
| Playlist with removed/private/unavailable entries | Source `32` plus Rust retryability coverage | Inspected unavailable playlist row and kept automated retryability test for deterministic filtering. | Removed rows show `removed_from_playlist`; private/auth/member/age/geo/deleted rows are not retried until auth/settings allow it; unlinked rows are excluded from analysis; detail displays unavailable count. | Partial pass | Fixture row `manual-unlinked-video` showed `no_captions`, `videoSourceId = null`, and unavailable count `1`. Rust test `retryable_playlist_video_rows_excludes_auth_deleted_and_removed_entries` covers skipped retry statuses. Live removed/private playlist rows were not available. |
| Direct video first, then playlist containing the same video | Source `30` video and source `32` playlist | Added the direct video first, then inspected playlist membership/detail. | Playlist row links to the existing canonical video source; no duplicate video source is created; `open source` selects the existing source. | Pass | Playlist row position `1` has `videoId = M7lc1UVf-VE` and `videoSourceId = 30`; video detail lists membership in playlist source `32`. |
| Comments-heavy video with cancellation | Source `30` comments sync | Started comments-only sync, requested cancel immediately, polled jobs. Added a regression test for cancel-finalization state. | Comment fetch respects max comments limit; cancellation moves `cancel_requested` to `cancelled` or clear terminal failed state; partial comment rows remain valid; UI clears active pending state after terminal status. | Pass | Initial manual run exposed a defect where `cancel_requested` could become `succeeded`. Added `job_state_finishes_cancel_requested_jobs_as_cancelled`; after the fix, live `source-job-2` ended `cancelled` with `Source job cancelled.` Existing comments remained valid (`itemCount = 94`). |
| Saved analysis run, then resync transcript in another caption language | Not exercised | No saved YouTube analysis run was created in this pass. | Saved snapshot metadata and trace refs remain stable; new transcript sync updates live source items without mutating saved run rows; timestamp trace links still resolve. | Not run | Requires a configured LLM profile and a saved YouTube report fixture. Automated trace/corpus tests cover timestamp refs and YouTube snapshot fields, but the live saved-run workflow still needs a manual pass. |
| App restart during active YouTube job | Source `30`, comments-only job | Started `source-job-3` comments sync, stopped the running Tauri app process, restarted dev app, inspected jobs/detail, then started a new metadata sync. | Jobs are in memory and not restored; `list_source_jobs` has no old active job after restart; UI state is not stuck; completed DB writes remain visible; user can start a new sync; no resume attempt is made. | Pass | Before shutdown, `source-job-3` was `running`. After restart, `list_source_jobs` returned `[]`, video detail still showed captions/comments from SQLite, and a new metadata `source-job-1` for source `30` completed `succeeded`. |

## Defects Found

- Cancellation race: if the user requested cancellation while an uninterruptible provider call completed successfully, `finish_job` could overwrite `cancel_requested` with `succeeded`. Fixed by making `SourceJobState::finish_job` preserve cancellation as the final terminal status. Regression test: `youtube::jobs::tests::job_state_finishes_cancel_requested_jobs_as_cancelled`.
