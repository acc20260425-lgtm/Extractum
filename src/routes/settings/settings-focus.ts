export const SETTINGS_FOCUS = Object.freeze({
  sections: Object.freeze(["llm-provider-profiles", "provider-test-runs"]),
  description: "Settings stay focused on LLM provider profiles and test runs.",
});
